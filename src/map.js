import React, { Component } from 'react';

class Map extends Component {
    state = {
  }
  render() {
    return (
      <div className="map" id="map">
       
      </div>
    );
  }
}

export default Map;
